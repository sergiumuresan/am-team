@model IEnumerable<TheAMTeam.Business.Models.TeamModel>
@{
    ViewBag.Title = "Teams";
    Layout = "~/Views/Shared/_Layout.cshtml";
    int pages = 0;
}
<h2>Teams</h2>
@*<form class="navbar-form navbar-left" role="search" action="/Team/Teams" method="post">
    <div id='search-form' class="form-group">
        <div class="input-group">
            <span id='search-icon' class="input-group-addon"><span class='glyphicon glyphicon-search'></span></span>
            <input type="text" class="form-control" placeholder="Search">
        </div>
    </div>
</form>*@

<div class="navbar navbar-inverse navbar-static-top" role="navigation">

    <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" rel="home" href="/">Logo</a>
    </div>

    <div class="collapse navbar-collapse navbar-ex1-collapse">

        <ul class="nav navbar-nav">
            <li><a href="/team/teams?page=1">Teams</a></li>
            @if (this.User.IsInRole("Admin"))
            {
                <li><a href="/team/AddTeam">AddTeam</a></li>
            }


            <li><a href="/">Main Page</a></li>
            
        </ul>

        <div class="pull-right">
            <form class="navbar-form pull-right" role="search" action="/Team/Teams" method="post">
                <div class="input-group">
                    <input type="text" class="form-control" placeholder="Search" name="search" id="search">
                    <div class="input-group-btn">
                        <button class="btn btn-default" type="submit"><i class="glyphicon glyphicon-search"></i></button>
                    </div>
                </div>
            </form>
        </div>

    </div>
</div>



<table class="table">
    <thead>
    <th>Name</th>
    <th>City</th>
    <th>Coach</th>
    <th>Players</th>
    </thead>
    <tbody>
        @foreach (var team in Model)
        {
            pages++;
            <tr>
                <td>@team.Name</td>
                <td>@team.City</td>
                <td>@team.Coach</td>
                <td>
                    <select>
                        @foreach (var item in team.PlayersModel)
                        {
                            <option>@item.Name</option>
                        }
                    </select>
                </td>
                <th>
                    @if (this.User.IsInRole("Admin"))
                    {
                        @Html.ActionLink("Asign Player", "AsignPlayer", "Team", new { teamId = team.TeamId }, null)
                    }

                </th>
                <th>
                    @if (this.User.IsInRole("Admin"))
                    {
                        @Html.ActionLink("Edit", "EditTeam", "Team", new { id = team.TeamId }, null)
                    }
                    </th>
                <th>
                    @if (this.User.IsInRole("Admin"))
                    {
                        @Html.ActionLink("Delete", "RemoveTeam", "Team", new { id = team.TeamId }, null)
                    }
                    </th>
            </tr>
        }
    </tbody>
</table>
@if (pages == 0)
{
    <p>No Teams Found!</p>
}
<input type="button" class="btn btn-default" value="Back" onclick="location.href='@Url.Action("Teams", "Team", new { page = Request.QueryString["page"].AsInt()>1?  Request.QueryString["page"].AsInt()-1 : 1  })'" />
<input type="button" class="btn btn-default" value="Next" onclick="location.href='@Url.Action("Teams", "Team", new { page = Request.QueryString["page"].AsInt()<=pages/4? Request.QueryString["page"].AsInt() +1 :Request.QueryString["page"].AsInt() })'" />